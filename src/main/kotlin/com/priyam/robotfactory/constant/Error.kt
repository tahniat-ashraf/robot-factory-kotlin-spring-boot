package com.priyam.robotfactory.constant

enum class Error(
    val errorMessage: String
) {
    INVALID_REQUEST("Invalid request! Please make sure to order exactly one part from each of the four categories (face, material, arms, mobility).")
}